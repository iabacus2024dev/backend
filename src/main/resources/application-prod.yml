spring:
  config:
    activate:
      on-profile: prod

  datasource:
  # TODO: 운영환경 데이터베이스 설정

  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    com.iabacus.salespro: info

decorator:
  datasource:
    p6spy:
      enable-logging: false

base:
  url: ${PROD_BASE_URL}